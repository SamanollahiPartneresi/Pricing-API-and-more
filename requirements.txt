# Runtime dependencies for the "Service Pricing Tool" Streamlit data app when
# deployed from this Git repository (entrypoint: pricing_rules_calculator/main.py).
# Floors are chosen so the UI APIs the app uses are available:
#   - st.columns(border=...) and st.dataframe(width="stretch")  -> streamlit >= 1.49
streamlit>=1.49
pandas>=2.0
httpx>=0.27
requests>=2.32
